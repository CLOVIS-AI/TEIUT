class Player extends AliveEntity{
  
  
  Player(int maxHealth, int defense, int maxMana, int strength, String name, Classe classe){
    super(maxHealth, defense, maxMana, strength, name, classe);
  }
}